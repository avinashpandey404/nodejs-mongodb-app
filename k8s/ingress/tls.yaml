apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: node-cert
  namespace: devops
spec:
  secretName: tls-secret
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - yourdomain.com
